FROM golang:1.21-alpine

WORKDIR /app

RUN apk add --no-cache git

# Install air for hot reload (optional, for development)
RUN go install github.com/cosmtrek/air@latest

COPY go.mod go.sum ./
RUN go mod download

COPY . .

EXPOSE 8080

CMD ["go", "run", "cmd/server/main.go"]
